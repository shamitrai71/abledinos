[build]
  publish = "."
  functions = "netlify/functions"

# Admin panel route
[[redirects]]
  from = "/admin"
  to = "/admin/index.html"
  status = 200

# SPA fallback for clean URLs
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
